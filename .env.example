# ================================
# 法律科技新闻Bot - 配置文件模板
# ================================
# 使用说明：
# 1. 复制此文件，重命名为 .env
# 2. 填写下面各项的API密钥和配置
# 3. 保存文件（不要提交到git等代码仓库）

# ----------------
# 必需配置（必须填写）
# ----------------

# NewsAPI 配置
# 获取地址：https://newsapi.org/register
# 免费版：每天最多100次请求
NEWS_API_KEY=your_newsapi_key_here

# Claude API 配置
# 获取地址：https://console.anthropic.com/
# API密钥在：Account Settings -> API Keys
# 建议使用Claude 3 Haiku模型，成本低
CLAUDE_API_KEY=your_claude_api_key_here

# 飞书机器人配置
# 获取步骤：
# 1. 在飞书群中添加自定义机器人
# 2. 获取Webhook URL
# 3. 填入下方（不要泄露给他人）
FEISHU_WEBHOOK_URL=https://open.feishu.cn/open-apis/bot/v2/hook/your_webhook_url_here

# ----------------
# 可选配置（有默认值，可以不填）
# ----------------

# 运行模式：test 或 production
# test: 立即执行一次（用于测试配置是否正确）
# production: 定时运行，每天中午12点执行（用于生产环境）
# 默认：production
RUN_MODE=test

# 是否启用备用方案
# 当Claude API调用失败时，是否使用简单的格式化方案
# true: 启用（推荐），false: 禁用
# 默认：true
ENABLE_FALLBACK=true

# 新闻搜索关键词
# 使用 OR 连接多个关键词，扩大搜索范围
# 默认：legal tech OR legal technology OR lawtech OR legal AI
SEARCH_KEYWORDS=legal tech OR legal technology OR lawtech OR legal AI

# 每次获取的新闻数量
# 免费版最多100条，建议设置为10-20条
# 默认：10
MAX_ARTICLES=10

# ================================
# 注意事项：
# ================================
# 1. 必需配置项缺一不可，否则程序无法运行
# 2. API密钥相当于密码，请妥善保管，不要分享给他人
# 3. .env文件应该添加到.gitignore，避免提交到代码仓库
# 4. NewsAPI有请求频率限制，免费版每天最多100次请求
# 5. Claude API按使用量计费，Haiku模型成本较低，大约$0.25/百万输入token
# 6. 建议先用 RUN_MODE=test 测试配置，确认无误后再改为 production
# 7. 程序会自动记录API使用次数，接近限制时会发出警告
